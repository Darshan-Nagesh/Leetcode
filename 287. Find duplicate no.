class Solution {
    public int findDuplicate(int[] nums) {
        int key=0;
        for(int i=0;i<nums.length;i++)
            for(int j=i+1;j<nums.length;j++)
                if(nums[i] == nums[j])
                    key = nums[i];
        return key;
    }
}
